node{
    if(env_BRANCH_NAME == 'master'){
        stage('Build Master'){
            echo "Building Master"
        }
    }

    if(env_BRANCH_NAME == 'dev'){
        stage('Build Dev'){
            echo "Building Dev"
        }
    }
}
